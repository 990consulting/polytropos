from typing import Iterable, Dict
class Track:

    @classmethod
    def build(cls, specs: Iterable[Dict]):
        """Convert specs into a Variable hierarchy, then construct a Track instance."""
        pass
